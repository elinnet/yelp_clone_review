<%= form_for [@restaurant, @review] do |review| %>
  <%= review.label :thoughts %>
  <%= review.text_area :thoughts %>
  <%= review.label :rating %>
  <%= review.select :rating,(1..5) %>

  <%= review.submit "Create Review" %>
<% end %>
